# Pin typescript-action-renovate-config

This task pins the version of `int128/typescript-action-renovate-config` used in Renovate configuration to a specific version (`v1.9.0`).

The workspace is already migrated by [precondition.sh](./precondition.sh).
Your task is to return a pull request description.
